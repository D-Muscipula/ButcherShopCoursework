spring:
  datasource:
    #url: jdbc:postgresql://localhost:5437/
    #url: jdbc:postgresql://db:5432/butcher_shop
    url: jdbc:postgresql://localhost:5432/butcher_shop
    username: ${USERNAME}
    password: ${PASSWORD}
    driverClassName: org.postgresql.Driver

token:
  signing:
    key: ${KEY}